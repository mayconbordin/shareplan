<% content_for :title do %>
  <title>SharePlan - Histórico</title>
<% end %>

<% content_for :styles do %>
	<%= stylesheet_link_tag(
		"reset",
		"facebox",
		"style",
		"jquery.dataTables")
	%>
<% end %>

<% content_for :scripts do %>
	<%= javascript_include_tag(
		"jquery-1.6.2.min",
		"jquery.dataTables.min",
		"jquery.facebox",
		"util",
		"model",
		"view",
		"controller")
	%>
	<script type="text/javascript">
		$(document).ready(function () {
			Controller.History.index();
			Controller.User.logout();
		});
	</script>
<% end %>

<!-- Main Container -->
<div class="main-container">
	
	<div id="history-title">
		<h1>Histórico</h1>
		<a id="new-history-button" class="green-button" href="<%= url_for(:action => 'new_step_one', :controller => 'history') %>" title="Adicionar histórico">Adicionar DRE</a>
	</div>
	
	<% if @status %>
	<ul class="messages">
		<% if @status == "success" %>
		<li class="success"><p>O DRE foi salvo com sucesso!</p><span class="close"></span></li>
		<% end %>
	</ul>
	<% end %>
	
	<!-- Histórico -->
	<div id="history" class="container-box">
		<div id="history-table-container" class="table-container">
			<table cellpadding="0" cellspacing="0" border="0" class="display" id="history-table"></table>
		</div>
	</div>
	<!-- Histórico -->

</div>
<!-- Main Container -->